version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      time: "07:00"
      timezone: "Europe/Amsterdam"
    # Ends in a paren, so dependabot appends the colon itself.
    commit-message:
      prefix: "build(deps)"
